{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "C Compile",
            "type": "shell",
            "command": "gcc",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${workspaceFolder}/hello"
            ],
            "group": "build"
        }
    ]
}
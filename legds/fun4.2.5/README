fn4.2.5描述

main描述
------->行为
	>先用堆存储初始化的数据
	>再逐一读命题字符串，调用Operat,记录该字符串的真假值
	>最后，根据TrueValue里的内容，打印结果

Operat描述
------->原型	status Operat(char *str,Heap *heap);
------->目的
	>判断str指向的命题的类型，并求出其的真假值
------->行为
	>将str命题与四类命题的一般形式逐一匹配.产生str命题的类型值，
	>再根据类型值，调用相关的方法，求出str的真值
